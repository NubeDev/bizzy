package api

// Store apps are DB-only. No disk files are written or read for store apps.
// The database (StoreApp with tools, prompts, uiComponents as JSON columns)
// is the single source of truth.
