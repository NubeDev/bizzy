{
  "name": "list_node_types",
  "description": "List all available node types (pallet) that can be created in Rubix.",
  "toolClass": "read-only",
  "params": {
    "category": {
      "type": "string",
      "required": false,
      "description": "Filter by category (e.g. 'core', 'ui', 'mqtt', 'bacnet')"
    }
  }
}
