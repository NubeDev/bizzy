{
  "name": "trip_planner",
  "description": "Plan a trip based on live weather. Gets weather for a city, then suggests activities, what to pack, and rates the day for outdoor plans.",
  "toolClass": "read-only",
  "params": {
    "activity": {
      "type": "string",
      "required": false,
      "description": "Preferred activity type: beach, hiking, sightseeing, skiing (optional)"
    },
    "city": {
      "type": "string",
      "required": true,
      "description": "City to plan a trip for"
    }
  }
}