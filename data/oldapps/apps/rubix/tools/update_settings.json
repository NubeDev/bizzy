{
  "name": "update_settings",
  "description": "Update settings on a Rubix node. Merges with existing settings (PATCH). Lookup by ID or name.",
  "toolClass": "read-write",
  "params": {
    "id": {
      "type": "string",
      "required": false,
      "description": "Node ID"
    },
    "name": {
      "type": "string",
      "required": false,
      "description": "Search by name (if id not provided)"
    },
    "settings": {
      "type": "string",
      "required": true,
      "description": "JSON string of settings to merge (e.g. '{\"enabled\": true, \"interval\": 5000}')"
    }
  }
}
