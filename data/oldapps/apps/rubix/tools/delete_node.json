{
  "name": "delete_node",
  "description": "Delete a Rubix node by ID or name. Cascades to child nodes. Returns what was deleted.",
  "toolClass": "destructive",
  "params": {
    "id": {
      "type": "string",
      "required": false,
      "description": "Node ID to delete"
    },
    "name": {
      "type": "string",
      "required": false,
      "description": "Search by name (if id not provided). Must match exactly one node."
    }
  }
}
